# Dependencies pre-installed into the BUNDLED Python (vendor/python) by
# scripts/seed-python.ps1, so ppt-master runs offline with zero pip install.
#
# Profile: ppt-master "svglib path" (no native cairo). Heavy/niche extras are
# intentionally omitted to control bundle size — a user can pip-install them
# into a project .venv on demand (the guard allows .venv installs):
#   - cairosvg      (needs native cairo libs on Windows)
#   - nbconvert     (pulls the full Jupyter stack; .ipynb source is niche)
#   - curl_cffi     (WeChat TLS bypass; web_to_md falls back to requests/node)
#   - pandoc        (external binary; legacy .doc/.odt/.rtf conversion)

# --- Core pipeline: SVG -> PPTX export ---
python-pptx>=0.6.21
svglib>=1.5.0
reportlab>=4.0.0

# --- Source -> Markdown conversion ---
PyMuPDF>=1.23.0
mammoth>=1.6.0
markdownify>=0.11.6
ebooklib>=0.18
openpyxl>=3.1.0
requests>=2.31.0
beautifulsoup4>=4.12.0

# --- Image processing ---
Pillow>=9.0.0
numpy>=1.20.0

# --- Optional narration / AI image backends (importable; need keys/network) ---
edge-tts>=7.2.8
openai>=1.0.0
google-genai>=1.0.0
